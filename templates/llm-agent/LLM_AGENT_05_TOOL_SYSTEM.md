# LLM Agent 工具系统分析

> 🔍 分析 Agent 的工具定义、调用机制、错误处理、安全沙箱

## 📋 核心问题清单

### 1. 工具定义与注册

#### 1.1 工具接口
- [ ] **工具 Schema**
  - 参数定义（JSON Schema/Pydantic/TypeScript）？
  - 参数验证机制？
  - 默认值处理？
  
- [ ] **工具描述**
  - 名称与描述（供 LLM 理解）？
  - 参数说明？
  - 返回值说明？
  - 使用示例？

#### 1.2 工具注册
- [ ] **注册方式**
  - 装饰器注册（@tool）？
  - 类继承注册？
  - 配置文件注册？
  - 动态注册？
  
- [ ] **注册发现**
  - 自动扫描？
  - 插件加载？
  - 热插拔？

#### 1.3 工具分类
- [ ] **内置工具**
  - 搜索工具？
  - 计算工具？
  - 文件操作？
  - 代码执行？
  
- [ ] **自定义工具**
  - 用户自定义？
  - 第三方集成？
  - API 封装？

### 2. 工具选择与调用

#### 2.1 工具选择
- [ ] **LLM 选择**
  - Function Calling？
  - ReAct 模式？
  - 工具描述匹配？
  
- [ ] **规则选择**
  - 基于意图路由？
  - 基于上下文？
  - 基于权限？

#### 2.2 调用机制
- [ ] **调用方式**
  - 同步调用？
  - 异步调用？
  - 流式调用？
  
- [ ] **参数传递**
  - JSON 参数？
  - 位置参数？
  - 上下文注入？

#### 2.3 并行调用
- [ ] **并行策略**
  - 多工具并行？
  - 依赖分析？
  - 结果聚合？

### 3. 工具执行

#### 3.1 执行环境
- [ ] **沙箱隔离**
  - 进程沙箱？
  - 容器沙箱（Docker）？
  - WASM 沙箱？
  
- [ ] **资源限制**
  - CPU 限制？
  - 内存限制？
  - 网络限制？
  - 时间限制？

#### 3.2 执行监控
- [ ] **进度追踪**
  - 执行状态？
  - 进度回调？
  - 超时检测？
  
- [ ] **日志记录**
  - 输入日志？
  - 输出日志？
  - 错误日志？

### 4. 错误处理

#### 4.1 错误分类
- [ ] **错误类型**
  - 参数错误？
  - 执行超时？
  - 资源不可用？
  - 权限不足？
  - 业务错误？

#### 4.2 错误恢复
- [ ] **重试机制**
  - 自动重试？
  - 重试策略（指数退避）？
  - 最大重试次数？
  
- [ ] **降级策略**
  - 备选工具？
  - 简化执行？
  - 缓存结果？

#### 4.3 错误反馈
- [ ] **错误信息**
  - 错误消息格式化？
  - 错误码定义？
  - 堆栈追踪？
  
- [ ] **LLM 反馈**
  - 错误传递给 LLM？
  - 让 LLM 修正参数？
  - 让 LLM 选择其他工具？

### 5. 安全与权限

#### 5.1 输入验证
- [ ] **参数验证**
  - 类型检查？
  - 范围检查？
  - 格式检查？
  
- [ ] **注入防护**
  - SQL 注入防护？
  - 命令注入防护？
  - 路径遍历防护？

#### 5.2 权限控制
- [ ] **访问控制**
  - 工具级权限？
  - 参数级权限？
  - 用户级权限？
  
- [ ] **审批机制**
  - 敏感操作审批？
  - 人工确认？
  - 白名单/黑名单？

#### 5.3 输出安全
- [ ] **输出过滤**
  - 敏感信息过滤？
  - 数据脱敏？
  - 格式校验？
  
- [ ] **结果限制**
  - 结果大小限制？
  - 执行时间限制？
  - 资源使用限制？

### 6. 工具生态

#### 6.1 标准协议
- [ ] **MCP (Model Context Protocol)**
  - MCP 服务器支持？
  - MCP 工具发现？
  - MCP 资源访问？
  
- [ ] **OpenAPI/Swagger**
  - API 自动转换？
  - Schema 导入？

#### 6.2 工具市场
- [ ] **工具共享**
  - 工具 marketplace？
  - 工具版本管理？
  - 工具评分？

## 🔍 代码检查点

### 工具定义
```bash
# 查找工具装饰器
rg "@tool|@action|@function" --type py

# 查找工具类
rg "class.*Tool|class.*Action" --type py
```

### 工具调用
```bash
# 查找 Function Calling
rg "function_call|tool_call|tools=" --type py

# 查找工具执行
rg "execute_tool|run_tool|invoke" --type py
```

### 安全沙箱
```bash
# 查找沙箱实现
rg "sandbox|docker|subprocess|isolation" --type py

# 查找权限控制
rg "permission|auth|whitelist|blacklist" --type py
```

### MCP 集成
```bash
# 查找 MCP 相关
rg "mcp|MCP|ModelContextProtocol" --type py
```

## 📊 评估标准

| 维度 | 优秀 (5分) | 良好 (4分) | 一般 (3分) | 需改进 (1-2分) |
|------|-----------|-----------|-----------|---------------|
| **工具定义** | Schema 完善，描述清晰 | 有 Schema | 简单定义 | 硬编码 |
| **调用机制** | 支持并行，异步 | 异步调用 | 同步调用 | 硬编码 |
| **错误处理** | 完善重试降级 | 有重试 | 简单错误 | 无处理 |
| **安全沙箱** | 容器隔离 | 进程隔离 | 有限隔离 | 无隔离 |
| **生态集成** | MCP + OpenAPI | MCP 支持 | 自定义 | 无标准 |

## 📝 分析输出模板

```markdown
# [项目名] - 工具系统分析

## 1. 工具定义

**定义方式**: 装饰器 / 类继承 / 配置

**工具 Schema**:
```python
@tool
def search(query: str, max_results: int = 10) -> List[Result]:
    """搜索工具
    
    Args:
        query: 搜索关键词
        max_results: 最大结果数
    
    Returns:
        搜索结果列表
    """
    ...
```

## 2. 工具清单

| 工具名 | 功能 | 参数 | 风险级别 |
|--------|------|------|----------|
| search | 网络搜索 | query, max_results | 低 |
| execute | 代码执行 | code, language | 高 |
| file_read | 文件读取 | path | 中 |

## 3. 调用机制

**调用流程**:
```
LLM 输出 → 解析工具调用 → 参数验证 → 执行工具 → 返回结果 → LLM 继续
```

**并行支持**: ✅ 支持多工具并行执行

## 4. 安全机制

**沙箱类型**: Docker 容器

**权限控制**:
- 高风险工具需要审批
- 文件操作限制在指定目录
- 网络访问白名单

## 5. 错误处理

**重试策略**: 指数退避，最多 3 次

**降级策略**: 搜索失败 → 使用缓存

## 6. 设计亮点

- ✅ **亮点1**: [具体说明]

## 7. 学习价值

- ⭐⭐⭐⭐⭐ [值得借鉴的设计]
```

## 8. 💡 设计洞察

> 从该项目的工具系统中提炼的可移植原则

### 8.1 工具设计原则

**原则1**: [原则名称]
- **原理**: [为什么重要]
- **证据**: [项目中的具体实现]
- **适用范围**: [什么场景适用]
- **去名检验**: ✅/⚠️ [去掉项目名后是否仍成立]

示例:
> **原则**: 工具并发分类应基于单次调用，而非工具类型
>
> **原理**: 同一工具对不同输入可能有不同的安全性（读文件 vs 写文件）
>
> **证据**: 项目对 file_read 工具，只读 glob 时标记为并发安全，写入操作时标记为串行
>
> **适用范围**: 任何支持工具并发的 Agent 系统
>
> **去名检验**: ✅ 通用原则

**原则2**: [同上格式]

### 8.2 安全设计原则

**原则1**: [原则名称]
- **原理/证据/适用范围/去名检验**

示例:
> **原则**: 工具权限默认失败关闭（fail-closed）
>
> **原理**: 忘记声明安全分类时，默认最严格 posture 是安全的；反之则可能危险
>
> **证据**: 工具注册时 concurrency_safe 默认为 false，read_only 默认为 false
>
> **去名检验**: ✅ 适用于所有权限系统

## 9. ⚠️ 隐含陷阱

> 从源码分析中发现的非显而易见的工具系统陷阱

### 9.1 调用陷阱

**陷阱1**: [陷阱名称]
- **现象**: [如果做错了会看到什么]
- **原因**: [为什么有这个陷阱]
- **正确做法**: [应该怎么做]

示例:
> **陷阱**: 并发工具调用的上下文修改是延迟的
>
> **现象**: 同一批并发工具执行后，某些工具看不到其他工具的上下文变更
>
> **原因**: 并发批内的上下文修改回调在整个批完成后才应用，不是每个工具完成后立即应用
>
> **正确做法**: 并发批内的工具不应依赖同批其他工具的上下文变更

### 9.2 安全陷阱

**陷阱1**: [同上格式]

## 🔗 参考项目

| 项目 | 工具系统特点 | 学习价值 |
|------|-------------|----------|
| **LangChain Tools** | 丰富的工具集 | ⭐⭐⭐⭐ 工具定义 |
| **OpenAI Function Calling** | 原生支持 | ⭐⭐⭐⭐⭐ 标准实现 |
| **MCP Servers** | 标准协议 | ⭐⭐⭐⭐⭐ 协议设计 |
| **OpenDevin** | 沙箱执行 | ⭐⭐⭐⭐⭐ 安全隔离 |

---

*模板版本: v2.0 - 增加设计洞察与隐含陷阱章节*
*最后更新: 2026-06-29*
